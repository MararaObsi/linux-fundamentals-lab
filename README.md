Step-by-Step Execution & Proof of Work

- i created a directory named `linux_test` and navigated into it.
- i created three empty files: `one.txt`, `two.txt`, and `three.txt`.
- then i listed the directory contents to confirm creation.

- Cmd i use:
-    mkdir linux_test
     cd linux_test
     touch one.txt two.txt three.txt
     cat to list the content

and then lastly i create a folder named image inside linux_test and then i take screenshot and save screenshot inside my picture folder and move it's content or 
screenshot.png to the directory i created or image .....

# Level 2: Linux Permissions

# 9. Create and Set Permissions
- Created `secret.txt`.
- Applied `chmod 640` to set restrictive permissions.

*Commands Executed:
```bash
touch secret.txt
chmod 640 secret.txt
ls -l secret.txt
